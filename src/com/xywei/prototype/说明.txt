2019.4.2 原型模式
复制原来类型的对象

浅克隆 ，只克隆基本数据类型， 
1,不会克隆引用类型？？（这点有疑问） 
2,当复制的时候，原型上的引用类型会不会也复制到目标类
3，当改变原型的时候，目标类是否也会被改变（区别深浅复制）

回答：
1，基本类型包括string类型都会被复制，但是引用类型会指向同一块地址
2，不会复制，但是会指向同一块地址
3，目标类的引用类型会被改变，但是基本类型不会


深克隆：
1，不仅复制基本类型，还复制引用类型
2，当原型基本类型改变的时候，目标类基本类型是否会改变？（疑问）
3，当原型的引用类型改变的时候，目标类的引用类型不会改变。
回答：
1，关键点是重新创建引用类型，重新setter
2，不会。
3，不会。